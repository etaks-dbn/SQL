SELECT P_CODE AS PRODUCT, INVOICE.INV_NUMBER AS InvoiceNumber, date_format(INV_DATE, '%Y-%m-%d') AS InvoiceDate
FROM invoice
JOIN LINE ON INVOICE.INV_NUMBER = LINE.INV_NUMBER
WHERE P_CODE IN (SELECT P_CODE FROM product WHERE P_PRICE > 100);
